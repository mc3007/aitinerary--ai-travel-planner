version: "3.9"

services:

  aitinerary:

    build:
      context: .
      args:
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL:-}

    container_name: aitinerary

    ports:
      - "3000:80"

    restart: unless-stopped